%   This code is used in section 4 for comparing two methods.


parpool(25)     
p = gcp;                
p.IdleTimeout = Inf;                                % start parallel pool with maximum workers  

P= Function_generatePositiveNormalMatrix (100000,16);           % check it before run
xx=8;yy=8;                                          % check it berfore run, xx refers to the number of inputs, yy refers to the number of outputs
                                                    % create a data matrix with specific density    

a=size(P,1);
a=a^0.5;
a=ceil(a);

 
tic;                                                                       
First_phase_EHD= Function_First_phase_EHD_Parallel(P, xx, yy, a);                   
elapsedTime_EHD = toc;                                                    % running EHD approach and record the time   


tic;                                                                     
First_phase_enhancedEHD= Function_First_phase_enhancedEHD_Parallel(P, xx, yy);                   
elapsedTime_twoD = toc;                                                   % running our approach and record the time  
 
